# 1. 구글 클라우드 서비스 계정(GCP) 생성 및 JSON 키 발급

파이썬에서 구글 시트를 자동으로 수정하려면, 마치 파이썬 프로그램에게 '가상의 구글 계정(서비스 계정)'을 만들어주고 
그 계정의 '비밀번호(JSON 키)'를 쥐여주는 과정이 필요합니다.

가장 먼저 세팅하셔야 할 GCP 서비스 계정 생성 및 JSON 키 발급 방법을 단계별로 안내해 드립니다. 천천히 따라와 주세요!

//============================

## 1단계: 구글 클라우드 콘솔 접속 및 새 프로젝트 생성
1. Google Cloud Console(https://console.cloud.google.com/)에 접속하여 구글 계정으로 로그인합니다.

2. 화면 왼쪽 상단의 [프로젝트 선택] (또는 기존 프로젝트 이름)을 클릭합니다.

3. 팝업창 오른쪽 위의 [새 프로젝트]를 클릭합니다.

4. 프로젝트 이름(예: sns-news-automation)을 입력하고 [만들기]를 클릭합니다.

5. 생성이 완료되면 다시 왼쪽 상단에서 방금 만든 프로젝트를 선택해 줍니다.

//============================

## 2단계: 필요한 API 활성화 (Sheets & Drive)
파이썬이 구글 시트 파일에 접근하려면 두 가지 API를 켜주어야 합니다.

1. 왼쪽 메뉴(햄버거 아이콘)를 열고 [API 및 서비스] > [라이브러리]로 이동합니다.

2. 검색창에 Google Sheets API를 검색하여 클릭한 후, 파란색 [사용] 버튼을 누릅니다.

3. 다시 라이브러리로 돌아가 Google Drive API를 검색하고, 동일하게 [사용] 버튼을 누릅니다. (시트 파일을 찾고 권한을 확인하는 데 필요합니다.)

//============================

## 3단계: 서비스 계정(Service Account) 만들기
1. 왼쪽 메뉴에서 [API 및 서비스] > [사용자 인증 정보]로 이동합니다.

2. 화면 상단의 [+ 사용자 인증 정보 만들기]를 클릭하고 [서비스 계정]을 선택합니다.

3. 서비스 계정 이름(예: news-bot)을 입력하고 [만들기 및 계속]을 누릅니다.

4. (선택사항) 역할 선택 창에서 [기본] > [편집자(Editor)]를 선택하고 [계속] ➔ [완료]를 누릅니다.

//============================

## 4단계: JSON 키 파일 다운로드
1. 방금 생성된 서비스 계정(이메일 주소 형태)이 목록에 나타나면, 해당 이메일 주소를 클릭합니다.

2. 상단 탭에서 [키]를 클릭합니다.

3. [키 추가] ➔ [새 키 만들기]를 누릅니다.

4. 키 유형을 JSON으로 선택하고 [만들기]를 누릅니다.

5. 자동으로 컴퓨터에 .json 파일이 다운로드됩니다. 
(이 파일이 파이썬 코드에서 사용할 인증서입니다. 외부에 유출되지 않도록 작업 폴더에 안전하게 보관해 주세요.)

//============================

## 5단계: ★ 구글 시트에 편집자 권한 부여하기 (매우 중요)
1. 다운로드한 .json 파일을 열어보면 "client_email": "news-bot@sns-news-automation.iam.gserviceaccount.com" 같은 형태의 긴 이메일 주소가 있습니다. 이 주소를 복사합니다.

2. 데이터가 저장될 SNS 뉴스스크랩 구글 시트 파일을 웹 브라우저에서 엽니다.

3. 우측 상단의 초록색 [공유] 버튼을 누릅니다.

4. 사용자 추가 입력란에 방금 복사한 서비스 계정 이메일 주소를 붙여넣고, 권한을 [편집자]로 설정한 뒤 [전송]을 누릅니다.

//============================

여기까지 완료하셨다면 파이썬과 구글 시트를 연결할 완벽한 준비가 끝났습니다! 
발급받으신 JSON 키 파일 준비가 완료되시면, 
다음 단계로 이 환경에 맞춰 작동하는 실제 파이썬 전체 코드를 작성해 드리겠습니다.
//============================


