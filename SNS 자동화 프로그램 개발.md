//=================================
## SNS 자동화 배포 프로그램 개발(유튜브 숏츠 대본, 텍스트 게시물)
//=================================

make 워크플로우 방식으로 파이썬으로 SNS 자동화 프로그램을 만들려고 해.

계정 및 구글 API Key 사용 등 필요한 개발 요소를 정리해 주고 개발 요구서를 작성해줘.

//=================================

Make.com에서 구현하신 **워크플로우**와 **구글 시트 결과물**을 보니 데이터의 흐름과 목적이 아주 명확합니다.

이 파이프라인을 파이썬 스크립트로 전환하면 매일 **업로드하시는 유튜브 영상 콘텐츠**나 **봇 소개 자료**를 다양한 SNS 채널로 확산시키는 자동화 시스템으로 완벽하게 활용하실 수 있습니다. 
특히 평소 다루시는 PyQt5 기반의 커스텀 대시보드 환경에 이 요약 모듈을 통합한다면, 트레이딩 지표와 뉴스 이슈를 한 화면에서 모니터링하는 등 확장성도 매우 뛰어납니다.

성공적인 개발을 위해 필요한 계정 및 환경 요소들을 정리하고, 이를 바탕으로 한 개발 요구사항 정의서(PRD)를 작성해 드립니다.

//=================================

# 개발 요구사항 정의서(PRD)

//=================================

## 1. 사전 준비 및 개발 요소 (Prerequisites)
파이썬 환경에서 구글 서비스와 웹 데이터를 제어하기 위해 다음 요소들이 사전에 준비되어야 합니다.

//=================================

### Google AI Studio API Key:

용도: Gemini 1.5 Flash 모델 호출 (텍스트 요약 및 숏폼 대본 생성)

상태: 이미 발급받으신 Pro 환경의 키(AIzaSy...)를 재사용합니다. (단, 코드 내 직접 입력은 피하고 .env 파일로 분리하여 보안을 유지해야 합니다.)

//=================================

### Google Cloud Platform (GCP) 서비스 계정:

용도: 파이썬에서 구글 시트에 접근하고 데이터를 기록하기 위한 권한(OAuth 2.0 대신 백그라운드용 인증).

필요 작업: GCP 콘솔에서 **새 프로젝트 생성 ➔ Google Drive API 및 Google Sheets API 활성화 ➔ 서비스 계정(Service Account) 생성 ➔ JSON 형태의 비공개 키 파일 다운로드.**

시트 공유: 다운로드한 JSON 파일 내부의 client_email 주소를 기존 'SNS 뉴스스크랩' 구글 시트 파일에 편집자로 공유해 주어야 합니다.

//=================================

### 타겟 대상 뉴스 RSS URL 목록:   <----- 구글알리미에서 검색후 RSS로 경로 저장하여 사용함.

용도: 주기적으로 크롤링할 대상 (현재 연합뉴스, 문화일보 등의 RSS 피드 주소 배열화).

//=================================

### 파이썬 라이브러리(추가 기능):

1. feedparser (RSS 피드 파싱용)

2. requests, beautifulsoup4 (HTML 웹 크롤링 및 텍스트 정제용)

3. google-generativeai (Gemini API 연동)

4. gspread, oauth2client (구글 시트 연동)

5. python-dotenv (API 키 환경변수 관리)

//=================================

## 2. SNS 소셜 미디어 자동화 파이프라인 개발 요구서

### [1. 프로젝트 개요]
프로젝트명: RSS 기반 뉴스 요약 및 SNS 콘텐츠 자동 생성 파이프라인

목표: 특정 RSS 피드로부터 최신 뉴스를 수집하고, AI를 활용해 SNS 배포용(유튜브 숏츠 대본, 텍스트 게시물) 포맷으로 가공하여 구글 시트에 자동 적재하는 백그라운드 시스템 구축.

### [2. 시스템 아키텍처 및 데이터 플로우]
1. 데이터 수집 (Data Ingestion): 스케줄러가 RSS 피드를 확인하여 새로운 기사의 URL과 메타데이터(제목, 작성일)를 추출.  <------------------- 구글알리미에서 검색후 RSS로 경로 저장하여 사용함.

2. 데이터 정제 (Parsing & Cleaning): 기사 원문 URL에 **HTTP GET 요청**을 보내고, 불필요한 **HTML 태그 및 광고 요소를 제거**하여 **순수 본문 텍스트만 추출.**

3, AI 가공 (AI Processing): 정제된 본문을 Gemini 1.5 Flash 모델에 프롬프트와 함께 전송하여 핵심 요약 및 숏폼 스크립트 형태의 결과물 반환.

4. 데이터 적재 (Data Storage): 최종 가공된 데이터를 한국 표준시(KST) 기준 날짜 포맷으로 변환하여 지정된 구글 시트에 새로운 행(Row)으로 추가.


### [3. 세부 기능 요구사항 (Functional Requirements)]
1. REQ-01: RSS 파싱 모듈

지정된 다수의 RSS URL 목록을 순회하며 파싱해야 함.

이미 처리한 기사는 중복 수집하지 않도록 최근 수집 시간을 기록하거나 캐싱하는 로직이 포함되어야 함.

2. REQ-02: 웹 스크래핑 모듈

한국 언론사(연합뉴스, 문화일보 등) 사이트의 특성(인코딩, 동적 로딩 등)을 고려하여 BeautifulSoup을 통한 정확한 본문 타겟팅 수행.

3. REQ-03: 프롬프트 엔지니어링 및 AI 모듈

기존 Make 모듈에서 검증된 프롬프트를 시스템 프롬프트로 고정.

결과물은 반드시 1) 핵심 요약 2) SNS 숏폼용 텍스트 두 부분으로 구조화되어 반환되어야 함.

4. REQ-04: 구글 시트 연동 모듈

결과값은 A열(제목), B열(요약문), C열(URL), D열(작성일) 순서로 정확히 매핑되어 append_row 방식으로 기재되어야 함.

작성일 데이터는 반드시 YYYY-MM-DD HH:mm 형태의 KST 타임존 포맷으로 변환되어야 함.


### [4. 비기능 및 환경 요구사항 (Non-Functional Requirements)]
예외 처리(Error Handling): 특정 뉴스 사이트의 크롤링 방어나 API 호출 제한(Rate Limit) 발생 시 프로그램이 종료되지 않고 로그를 남긴 뒤 다음 항목으로 넘어가도록 try-except 블록을 구성해야 함.

보안: API Key 및 GCP JSON 키의 경로는 절대 하드코딩하지 않고 .env를 통해 주입받음.

확장성 (UI 연동 고려): 백그라운드 스케줄러 방식 외에도, 추후 PyQt5로 개발 중이신 하이브리드 대시보드의 특정 위젯이나 버튼(예: "수동 크롤링 및 요약 실행")과 연결할 수 있도록 핵심 로직을 클래스(Class) 형태로 모듈화하여 작성함.

//=================================

















